# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure(2) do |config|
  config.vm.define :ubuntu do |ubuntu|
    ubuntu.vm.box = "ubuntu/xenial64"
  end

  config.vm.define :macosx do |macOS|
    macOS.vm.box = "jhcook/macos-sierra"
  end
end
