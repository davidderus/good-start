# Usage: `use docker-machine my-machine` in a .envrc file
use_docker-machine() {
  local machine_name=$1
  eval $(docker-machine env $machine_name)
  echo -e "\033[0;32m$machine_name machine loaded!\033[0m"
}
